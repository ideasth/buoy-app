node_modules/
dist/
.vite/

# Generated at build time from client/public/MasterTemplateCalendar.xlsx by
# scripts/build-master-template.cjs (run as part of `npm run build`).
client/src/generated/

# SQLite database files — never commit; exposes demo data at rest
data.db
data.db-shm
data.db-wal
data.db-journal

.env
.env.*
!.env.example

*.log
.DS_Store

# Build-time secret injection — committed file is empty stub; bake-secret.ts overwrites it before publish
server/baked-secret.ts
server/baked-llm-keys.ts

# Bootstrapped runtime secrets — never commit
.secrets/

# Operational VPS-local files (created at deploy time or by pm2 setup)
.deploy-backups/
ecosystem.config.cjs
start.sh
start.sh.bak
data.db.pre-stage*
